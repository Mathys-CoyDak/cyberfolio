{% extends 'base.html.twig' %}

{% block title %}Créer la technologie{% endblock %}

{% block body %}
    <h1>Créer la technologie</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.version) }}
    {{ form_row(form.logo) }}

    <button class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
{% endblock %}