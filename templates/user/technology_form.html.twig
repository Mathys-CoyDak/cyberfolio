<h1>{{ form.vars.value.id ? 'Modifier la technologie' : 'Ajouter une technologie' }}</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
<button type="submit">{{ form.vars.value.id ? 'Modifier' : 'Ajouter' }}</button>
{{ form_end(form) }}
